# 운영 통계 — claude-pii-guard

> **초기 운영 데이터.** 표본이 작다(운영 세션 2개 · 5일). 사용량이 누적되는 대로 갱신한다.
> 생성: `node analysis/analyze_pii_log.js 30 markdown`
> 원본 로그: `~/.claude/logs/sensitive-prompt-scan.log` (JSON Lines)
>
> 비-UUID `sessionId`(수동 stdin 테스트 등)는 운영 통계를 오염시키므로 제외한다 —
> 아래 수치는 실제 Claude Code 세션만 집계한 것이다.

## 운영 실측 데이터 (2026-05-19 기준)

- 측정 기간: `2026-05-15` ~ `2026-05-19` (5일)
- 고유 운영 세션: **2**
- 테스트 세션 제외: 4건 (비-UUID sessionId)

### 이벤트 요약

| 이벤트 | 건수 | 일평균 |
|--------|------|--------|
| 차단 | 11 | 2.2 |
| 우회 | 1 | 0.2 |

### 카테고리별 차단 빈도

| 카테고리 | 건수 | 비율 |
|---------|------|------|
| RRN | 9 | 81.8% |
| STUDENT_NUM | 2 | 18.2% |

실제 사용 중 차단의 대부분(81.8%)이 RRN(주민등록번호) — 위협도가 가장 높은 카테고리에 차단이 집중된다.

### Bypass 분포

| 우회 경로 | 건수 |
|----------|------|
| token | 1 |

> 전체 이벤트 대비 우회 비율: **8.3%**.
> `via: token` 은 프롬프트에 `--allow-pii` 문자열이 포함됐음을 뜻한다. 로그는 토큰의 존재만 기록하므로
> 우회 *의도* 자체를 증명하지는 않는다. 환경변수 기반 영구 우회(`SENSITIVE_PROMPT_BYPASS`)는 0건.

### 일별 추이 (최근 5일)

| 날짜 | 차단 | 우회 |
|------|------|------|
| 2026-05-15 | 0 | 1 |
| 2026-05-16 | 0 | 0 |
| 2026-05-17 | 0 | 0 |
| 2026-05-18 | 8 | 0 |
| 2026-05-19 | 3 | 0 |

## 한계

- 표본이 작다(운영 세션 2개). 통계적 일반화가 아니라 "도구가 실제 환경에서 동작한다"는 증거로 읽을 것.
- 우회 탐지는 프롬프트의 `--allow-pii` 문자열 포함 여부에 기반한다. 도구 자체를 설명하는 프롬프트가
  해당 문자열을 포함하면 우회로 집계될 수 있다 — 표본이 커지면 false positive 여부를 재검토한다.
